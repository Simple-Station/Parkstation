- type: weightedRandom
  id: Secret
  weights:
    Traitor: 0.30
    # Wizard: 0.10 # Disabled until fixed (uplink whitelist broke)
    # Flawed: 0.15 # Shouldn't be by itself
    Zombie: 0.15
    Survival: 0.20
    Extended: 0.25
    # Nukeops: 0.00
